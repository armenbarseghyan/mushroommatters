{% comment %}
  Announcement Bar Section - Shopify Theme Editor Compatible
{% endcomment %}

{%- if section.settings.enabled != false -%}
  <div class="announcement-bar" data-announcement-bar data-section-id="{{ section.id }}">
    <div class="announcement-bar__container">
      <div class="announcement-bar__content">
        {%- if section.blocks.size > 0 -%}
          <div class="announcement-bar__messages" data-announcement-messages>
            {%- for block in section.blocks -%}
              {%- if block.settings.message != blank -%}
                <div class="announcement-bar__message" 
                     data-announcement-message 
                     {% if forloop.first %}data-active{% endif %}
                     {{ block.shopify_attributes }}>
                  {%- if block.settings.link != blank -%}
                    <a href="{{ block.settings.link }}" class="announcement-bar__link">
                      {{ block.settings.message }}
                    </a>
                  {%- else -%}
                    {{ block.settings.message }}
                  {%- endif -%}
                </div>
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- else -%}
          <div class="announcement-bar__message" data-announcement-message data-active>
            {{ section.settings.message }}
          </div>
        {%- endif -%}
      </div>
      
      {%- if section.settings.show_close_button -%}
        <button class="announcement-bar__close" 
                data-announcement-close 
                aria-label="Close announcement">
          <svg width="16" height="16" viewBox="0 0 16 16">
            <path d="M12 4L4 12M4 4l8 8" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
        </button>
      {%- endif -%}
    </div>
  </div>
{%- endif -%}

{% stylesheet %}
.announcement-bar {
  background-color: {{ section.settings.background_color | default: '#121212' }};
  color: {{ section.settings.text_color | default: '#ffffff' }};
  padding: 1.2rem 0;
  position: relative;
  z-index: 10;
  text-align: center;
}

.announcement-bar__container {
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
}

.announcement-bar__content {
  flex: 1;
}

.announcement-bar__messages {
  position: relative;
  overflow: hidden;
  height: 2.4rem;
}

.announcement-bar__message {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  opacity: 0;
  transform: translateY(100%);
  transition: all 0.3s ease;
  font-size: 1.4rem;
  font-weight: 500;
  line-height: 2.4rem;
  display: flex;
  align-items: center;
  justify-content: center;
}

.announcement-bar__message[data-active] {
  opacity: 1;
  transform: translateY(0);
}

.announcement-bar__link {
  color: inherit;
  text-decoration: none;
  transition: opacity 0.2s ease;
}

.announcement-bar__link:hover {
  opacity: 0.8;
}

.announcement-bar__close {
  position: absolute;
  right: 2rem;
  top: 50%;
  transform: translateY(-50%);
  background: none;
  border: none;
  color: inherit;
  cursor: pointer;
  padding: 0.5rem;
  border-radius: 50%;
  transition: background-color 0.2s ease;
  display: flex;
  align-items: center;
  justify-content: center;
}

.announcement-bar__close:hover {
  background-color: rgba(255, 255, 255, 0.1);
}

@media (max-width: 749px) {
  .announcement-bar {
    padding: 1rem 0;
  }
  
  .announcement-bar__container {
    padding: 0 1rem;
  }
  
  .announcement-bar__message {
    font-size: 1.2rem;
    line-height: 1.4;
    height: auto;
    min-height: 2rem;
  }
  
  .announcement-bar__messages {
    height: auto;
    min-height: 2rem;
  }
  
  .announcement-bar__close {
    right: 1rem;
    padding: 0.25rem;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "Announcement Bar",
  "tag": "section",
  "class": "shopify-section-group-header-group",
  "settings": [
    {
      "type": "checkbox",
      "id": "enabled",
      "label": "Enable announcement bar",
      "default": true
    },
    {
      "type": "text",
      "id": "message",
      "label": "Default message",
      "default": "Free shipping on orders over £50",
      "info": "Used when no announcement blocks are added"
    },
    {
      "type": "range",
      "id": "rotation_speed",
      "label": "Message rotation speed (seconds)",
      "min": 3,
      "max": 10,
      "step": 1,
      "default": 5,
      "info": "How long each message is displayed"
    },
    {
      "type": "checkbox",
      "id": "show_close_button",
      "label": "Show close button",
      "default": true
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#121212"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#ffffff"
    }
  ],
  "blocks": [
    {
      "type": "message",
      "name": "Announcement",
      "limit": 5,
      "settings": [
        {
          "type": "text",
          "id": "message",
          "label": "Message",
          "default": "Free shipping on orders over £50"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link (optional)",
          "info": "Make the announcement clickable"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Announcement Bar",
      "settings": {
        "enabled": true,
        "show_close_button": true,
        "rotation_speed": 5
      },
      "blocks": [
        {
          "type": "message",
          "settings": {
            "message": "Free shipping on orders over £50 🚚"
          }
        },
        {
          "type": "message",
          "settings": {
            "message": "New mushroom collection available now 🍄"
          }
        },
        {
          "type": "message",
          "settings": {
            "message": "Subscribe & Save 20% on all products ✨"
          }
        }
      ]
    }
  ]
}
{% endschema %}
